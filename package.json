{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app/server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha app/server/**/*.test.js --timeout 5000",
    "test-watch": "nodemon --exec \"npm test\"",
    "docker:build": "docker build -t node-chat-app .",
    "docker:start": "docker run -p 3000:3000  node-chat-app"
  },
  "engines": {
    "node": "9.5.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "moment": "^2.21.0",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "expect": "^23.0.0-alpha.0",
    "mocha": "^5.0.4",
    "nodemon": "^1.18.9"
  }
}
